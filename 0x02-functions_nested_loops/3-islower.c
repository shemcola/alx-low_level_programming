#include "main.h"

/**
 * _islower - Checks for lowercase character
 * @c: The character to  e chekced
 * Return: 1 for lowercase haracters or 0 for else
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
